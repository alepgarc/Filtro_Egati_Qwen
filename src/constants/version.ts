/**
 * Application Version Control
 * Version starts at 1.00.
 * Increment after the dot (1.01, 1.02, ...) on each implementation/update,
 * until the user requests major version changes (e.g., 2.00).
 */
export const APP_VERSION = '1.02';
